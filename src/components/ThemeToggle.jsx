import React from 'react';
import { Globe } from 'lucide-react';

const ThemeToggle = () => {
    // Theme toggle removed - using consistent dark theme
    // Keeping component for future use if needed
    return null;
};

export default ThemeToggle;
